# DEV ONLY — same Wi‑Fi testing (phone loads your PC's Next.js dev server).
# Copy to .env.capacitor.local and set your PC's LAN IP.
#
# For Play Store / internet app, use .env.capacitor.production.example instead.

CAPACITOR_SERVER_URL=http://192.168.29.125:3000
