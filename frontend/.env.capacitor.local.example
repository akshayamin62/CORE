# Copy to .env.capacitor.local and set your PC's LAN IP (same Wi‑Fi as your phone).
# Use the IPv4 from ipconfig for the Wi‑Fi adapter your PHONE uses (not VMware/virtual adapters).
# Must match the "Network:" URL shown when you run npm run dev:mobile.
#
# Then:
#   1. Backend:  cd ../backend && npm run dev
#   2. Frontend: npm run dev:mobile
#   3. npm run cap:sync
#   4. npm run cap:android

CAPACITOR_SERVER_URL=http://192.168.29.125:3000
